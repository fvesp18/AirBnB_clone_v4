#!/usr/bin/node
// Listens for changes on each input checkbox
$(document).ready(function () {
	if ($(input.checkbox).is(':checked') {
		let stored_id = "something goes here";
	}) else {
		stored_id.last().remove();
	}
})
